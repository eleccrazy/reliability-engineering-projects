global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "node_exporters"
    static_configs:
      - targets:
{% for host in groups['all'] %}
        - "{{ hostvars[host]['private_ip'] }}:9100"
{% endfor %}
